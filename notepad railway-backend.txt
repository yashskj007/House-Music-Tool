Read CLAUDE.md for context. Create a Railway Express.js backend that proxies all API calls. Do the following: (1) Create package.json with express, node-fetch, cors as dependencies and start script as "node server.js", (2) Create server.js - an Express server that: serves index.html as the root route, has a POST /api/claude route that reads ANTHROPIC_API_KEY from process.env and proxies to Anthropic API with web search tool enabled and streams the response back, has a GET /api/lastfm route that reads LASTFM_API_KEY from process.env and proxies to Last.fm API, has a GET /api/daily-limit route that tracks usage in memory and enforces 200 requests per day limit, (3) Update index.html to call /api/claude instead of direct Anthropic API, call /api/lastfm instead of Last.fm directly, remove all API key input fields from UI, (4) Update CLAUDE.md. Commit and push.